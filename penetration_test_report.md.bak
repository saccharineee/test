# 🔴 渗透测试报告

**目标**: http://192.168.80.129:5000
**时间**: 2026-07-07 04:37 EDT（首次） / 2026-07-08 04:35 EDT（更新）
**类型**: 黑盒渗透测试 + 代码审计

---

## 漏洞等级说明
- 🔴 **高危** - 可被远程利用导致数据泄露/系统控制
- 🟠 **中危** - 信息泄露/暴力破解/会话劫持
- 🟡 **低危** - 信息收集/配置问题

---

## 1️⃣ 🔴 高危漏洞

### 1.1 会话固定（Session Fixation）

**漏洞描述**: 登录前后 session cookie **完全相同**。攻击者可先获取一个有效 session，诱骗受害者使用该 session 登录，登录后 session 不变，攻击者可共享受害者 session。

**复现**:
```python
# 攻击者获取session
s = requests.Session()
s.get('http://target/login')
malicious_session = s.cookies['session']

# 攻击者将malicious_session发给受害者
# 受害者用该session登录
# 攻击者可以用自己的session访问受害者的账户
```

**影响**: 可能导致账号被劫持
**修复建议**: `session.regenerate()` 或 `del session['username'] + session.clear()` 后重新生成 session
**修复状态**: ✅ 已修复 — 登录时调用 `session_regenerate()` 清空并重建 session

### 1.2 SQL 注入（新增） — Register / Search

**漏洞描述**: `register` 和 `search` 路由使用 **f-string 拼接 SQL**，攻击者可通过注入特殊字符执行任意 SQL。

**复现**:
```
# 搜索框输入：' OR 1=1 --
# 实际执行：SELECT * FROM users WHERE username LIKE '%' OR 1=1 --%'
# 结果：泄露所有用户数据
```

**影响**: 全量用户数据泄露、数据篡改、甚至删库
**修复建议**: 全部改用参数化查询（`?` 占位符传参）
**修复状态**: ✅ 已修复 — 两处 f-string 拼接均已替换为参数化查询

---

## 2️⃣ 🟠 中危漏洞

### 2.1 用户枚举（User Enumeration）

**漏洞描述**: 已存在用户和不存在用户的登录失败响应 **长度不同**（响应渲染时 hashlib 计算时间差异导致微小差异），可通过响应内容差异枚举有效用户名。

**现状**: 当前版本错误信息相同，但速率限制被绕过时仍可区分

**影响**: 辅助暴力破解
**修复建议**: 确保两种情况的响应完全一致（包括响应时间）
**修复状态**: ✅ 已修复 — 统一错误信息"用户名或密码错误"

### 2.2 无速率限制 / 重启即重置

**漏洞描述**: 速率限制原基于进程内字典，**重启即重置**。攻击者可在每次限速后等待15分钟继续爆破。

**影响**: 暴力破解时间成本降低
**修复建议**: 使用 Redis 或持久化存储速率计数
**修复状态**: ✅ 已修复 — 速率计数持久化至 `data/rate_state.json`，重启不丢失

### 2.3 Server 信息泄露

**漏洞描述**: HTTP响应头泄露：
```
Server: Werkzeug/3.1.8 Python/3.13.12
```
暴露了 Werkzeug 和 Python 的具体版本号。

**影响**: 辅助漏洞利用（已知版本CVE搜索）
**修复建议**: 使用反向代理隐藏版本信息，或自定义响应头
**修复状态**: ✅ 已修复 — Server 头伪装为 `Kangle/3.1`

### 2.4 双数据库架构（新增）

**漏洞描述**: 项目维护了两套独立的 SQLite 数据库：
- `users.db`（主库）：用于登录验证，bcrypt 哈希密码
- `data/users.db`（教学库）：用于注册/搜索，**明文密码**

**问题**:
1. 注册写教学库，登录查主库 → **注册后无法登录**
2. 教学库存明文密码 → 注入即泄露所有凭据
3. 两套表结构不一致，增加维护复杂度

**影响**: 功能不可用 + 凭据泄露
**修复建议**: 统一为单数据库，全部走参数化查询，密码 bcrypt 哈希
**修复状态**: ✅ 已修复 — 已移除教学库 `data/users.db`，注册/搜索全部使用主库，密码 bcrypt 加密存储

### 2.5 /search 无认证访问（新增）

**漏洞描述**: 搜索接口无需登录即可使用，可搜索到所有用户信息。

**影响**: 任意访问者可枚举用户列表
**修复建议**: 要求登录后才能使用搜索
**修复状态**: ✅ 已修复 — `/search` 路由增加登录检测，未登录跳转 `/login`

---

## 3️⃣ 🟡 低危漏洞

### 3.1 无HTTPS

**漏洞描述**: 仅HTTP通信，密码/会话明文传输。

**影响**: 中间人攻击可窃取会话和密码
**修复建议**: 生产环境必须启用HTTPS
**修复状态**: ⏳ 未修复 — 依赖生产环境配置（Nginx + HTTPS）

### 3.2 无账户锁定

**漏洞描述**: 仅限速不锁定，攻击者可长期低速持续爆破。

**影响**: 长期持续攻击
**修复建议**: 多次失败后临时/永久锁定账户
**修复状态**: ✅ 已修复 — 5次失败锁定30分钟

### 3.3 CSRF Token 重放保护不足

**漏洞描述**: 登录后 CSRF token 更新，但未验证 token 与 session 的绑定关系。

**影响**: 复杂度低
**修复建议**: 绑定 token 到用户 IP 或 user-agent
**修复状态**: ⏳ 延期 — 当前通过 `secrets.compare_digest` 和每次请求刷新 token 已有一定防护

### 3.4 注册密码无强度要求（新增）

**漏洞描述**: 注册接口未对密码做长度/复杂度校验。

**影响**: 弱密码导致暴力破解难度降低
**修复建议**: 校验密码最小长度
**修复状态**: ✅ 已修复 — 注册密码最小 8 位

---

## 4️⃣ 安全防护状态（已正确配置）

| 防护项 | 状态 | 说明 |
|--------|------|------|
| Debug模式关闭 | ✅ | `debug=False` |
| 随机密钥 | ✅ | `secrets.token_hex(32)` |
| 密码哈希 | ✅ | bcrypt + 随机盐 |
| CSRF保护 | ✅ | Session绑定token |
| HttpOnly Cookie | ✅ | |
| SameSite Cookie | ✅ | Lax |
| 会话超时 | ✅ | 2小时 |
| 登录频率限制 | ✅ | 每IP 15分钟20次，持久化不丢失 |
| 错误信息一致性 | ✅ | 统一"用户名或密码错误" |
| 账户锁定 | ✅ | 5次失败锁定30分钟 |
| SQL注入防护 | ✅ | 全参数化查询 |
| Search 认证保护 | ✅ | 需登录 |
| 单数据库架构 | ✅ | 统一用户库，避免数据分裂 |

---

## 5️⃣ 总结

| 严重等级 | 数量 | 已修复 | 未修复 |
|----------|------|--------|--------|
| 🔴 高危 | 2 | 2 | 0 |
| 🟠 中危 | 5 | 5 | 0 |
| 🟡 低危 | 4 | 2 | 2* |

*\* 低危中 2 处未修复：HTTPS（需生产环境配置）、CSRF token 绑定增强（低风险延期处理）*

**本轮新增修复（2026-07-08）**:
1. ❌ 移除双数据库架构 → 统一为单一主库
2. ❌ 移除 `data/users.db` 明文密码存储 → bcrypt 哈希
3. ❌ `/search` 移除未认证访问 → 需登录
4. ❌ 速率限制从内存持久化到磁盘 → 重启不丢失
5. ❌ 注册接口增加密码强度校验
